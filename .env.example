# Copy this file to .env and fill it in. .env is ignored by git, so the password stays off GitHub.
# Leave DATABASE_URL out to use the local SQLite file (uc_menu.db) instead.
# Neon: project dashboard -> Connect -> copy the connection string.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/neondb?sslmode=require
